<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Approve Item"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="16dp"
        android:gravity="center" />

    <!-- Item Name -->
    <TextView
        android:id="@+id/tvApproveItemName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Item: iPhone 13 Pro"
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_marginBottom="8dp" />

    <!-- Item Status -->
    <TextView
        android:id="@+id/tvApproveItemStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Current Status: Pending"
        android:textSize="14sp"
        android:textColor="#666666"
        android:layout_marginBottom="16dp" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#CCCCCC"
        android:layout_marginBottom="16dp" />

    <!-- Notes Label -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Notes (Optional)"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="8dp" />

    <!-- Notes EditText -->
    <EditText
        android:id="@+id/etApproveNotes"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:hint="Add any notes about this approval..."
        android:textSize="14sp"
        android:padding="12dp"
        android:background="@drawable/edit_text_background"
        android:gravity="top|start"
        android:textColor="#000000"
        android:textColorHint="#999999"
        android:layout_marginBottom="16dp"
        android:inputType="textMultiLine" />

    <!-- Info Text -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ℹ️ This itemAdmin will be moved to the 'Active' category after approval."
        android:textSize="12sp"
        android:textColor="#FF9800"
        android:layout_marginBottom="16dp"
        android:padding="8dp"
        android:background="#FFF3E0"
        android:gravity="center" />

</LinearLayout>